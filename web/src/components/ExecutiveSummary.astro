<aside class="summary" aria-label="Resumen ejecutivo">
	<p class="summary__label">Respuesta ejecutiva</p>
	<div class="summary__content"><slot /></div>
</aside>

<style>
	.summary {
		max-width: 58rem;
		padding: var(--space-5);
		border: 1px solid #83aa9b;
		border-radius: var(--radius-lg);
		background: #e9f2ee;
		box-shadow: var(--shadow-sm);
	}

	.summary__label {
		margin: 0 0 var(--space-2);
		color: var(--color-brand);
		font-size: var(--step--1);
		font-weight: 800;
		letter-spacing: 0.09em;
		text-transform: uppercase;
	}

	.summary__content {
		font-family: var(--font-serif);
		font-size: var(--step-1);
		line-height: 1.45;
	}

	.summary__content :global(p) {
		margin: 0;
	}
</style>
